<section class="visual-bento" aria-labelledby="visual-bento-heading">
  <div class="visual-bento__intro">
    <p class="eyebrow">Built for momentum</p>
    <h2 id="visual-bento-heading">The systems behind your next reach.</h2>
    <p>Thoughtful interfaces, reliable automation, and production-grade foundations in one focused build partner.</p>
  </div>
  <div class="visual-bento__grid">
    <article class="visual-bento__card visual-bento__card--wide">
      <div class="visual-bento__copy"><span class="visual-bento__kicker">01 / Web apps</span><h3>Live client dashboards</h3><p>Multi-tenant portals that make complex operations feel simple.</p></div>
      <div class="visual-bento__image"><img src="/assets/bento-web-apps.webp" alt="Dark client portal dashboard with colorful metrics" width="1448" height="1086" loading="lazy" /></div>
      <span class="visual-bento__badge"><span class="status-dot status-dot--live"></span> Live Client Dashboards</span>
    </article>
    <article class="visual-bento__card visual-bento__card--wide">
      <div class="visual-bento__copy"><span class="visual-bento__kicker">02 / Automation</span><h3>Business &amp; AI workflows</h3><p>Connected systems that turn repetitive work into reliable flow.</p></div>
      <div class="visual-bento__image visual-bento__image--workflow"><img src="/assets/bento-ai-workflow.webp" alt="Node-based automation workflow connected by glowing amber lines" width="1448" height="1086" loading="lazy" /><span class="workflow-pulse workflow-pulse--one"></span><span class="workflow-pulse workflow-pulse--two"></span></div>
    </article>
    <article class="visual-bento__card">
      <div class="visual-bento__copy"><span class="visual-bento__kicker">03 / Technical depth</span><h3>APIs that hold up</h3><p>Clear architecture from interface to infrastructure.</p></div>
      <div class="visual-bento__image"><img src="/assets/bento-code-depth.webp" alt="Dark code editor with cyan and amber syntax highlighting" width="1448" height="1086" loading="lazy" /></div>
      <div class="stack-pills"><span>Next.js</span><span>Go</span><span>Python</span><span>PostgreSQL</span></div>
    </article>
    <article class="visual-bento__card visual-bento__card--metric">
      <span class="visual-bento__kicker">04 / Rapid execution</span>
      <strong>2–4 Week<br /><em>Launch Cycles</em></strong>
      <p>Short feedback loops. Working software early.</p>
      <div class="metric-progress"><span></span></div><div class="metric-labels"><span>Discovery</span><span>Launch ready</span></div>
    </article>
  </div>
</section>

<style>
  .visual-bento { max-width: 1152px; margin: 0 auto; padding: 5.5rem 1.5rem; }
  .visual-bento__intro { max-width: 660px; margin: 0 auto 3rem; text-align: center; }
  .visual-bento__intro h2 { margin: .5rem 0 .8rem; }
  .visual-bento__intro > p:last-child { color: #a1a1aa; font: 400 .95rem/1.7 var(--font-body); }
  .eyebrow, .visual-bento__kicker { color: #f59e0b; font: 700 .65rem/1.2 var(--font-mono); letter-spacing: .16em; text-transform: uppercase; }
  .visual-bento__grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 1rem; }
  .visual-bento__card { position: relative; overflow: hidden; min-height: 350px; padding: 1.35rem; border: 1px solid rgba(255,255,255,.08); border-radius: 20px; background: linear-gradient(135deg, rgba(255,255,255,.055), rgba(255,255,255,.012)); box-shadow: 0 18px 50px rgba(0,0,0,.22); transition: border-color .25s ease, transform .25s ease, box-shadow .25s ease; }
  .visual-bento__card:hover { transform: translateY(-4px); border-color: rgba(245,158,11,.28); box-shadow: 0 24px 70px rgba(0,0,0,.4), 0 0 60px rgba(245,158,11,.05); }
  .visual-bento__card--wide { min-height: 410px; }
  .visual-bento__copy { position: relative; z-index: 2; max-width: 285px; }
  .visual-bento h3 { margin: .55rem 0 .45rem; font-size: 1.25rem; }
  .visual-bento__copy p, .visual-bento__card--metric p { color: #a1a1aa; font: .78rem/1.6 var(--font-body); }
  .visual-bento__image { position: absolute; inset: 8.5rem 0 0; opacity: .82; mask-image: linear-gradient(to bottom, transparent 0%, black 16%, black 100%); }
  .visual-bento__image img { width: 100%; height: 100%; object-fit: cover; object-position: center; }
  .visual-bento__badge { position: absolute; right: 1rem; bottom: 1rem; display: inline-flex; align-items: center; gap: .45rem; padding: .55rem .7rem; border: 1px solid rgba(255,255,255,.11); border-radius: 999px; background: rgba(9,9,10,.75); backdrop-filter: blur(12px); color: #d4d4d8; font: 600 .62rem var(--font-mono); }
  .workflow-pulse { position: absolute; width: 12px; height: 12px; border-radius: 50%; background: #fbbf24; box-shadow: 0 0 0 8px rgba(251,191,36,.12), 0 0 22px rgba(251,191,36,.9); animation: workflow-pulse 2.2s ease-in-out infinite; }
  .workflow-pulse--one { top: 43%; left: 48%; }.workflow-pulse--two { top: 62%; left: 70%; animation-delay: .8s; }
  @keyframes workflow-pulse { 50% { transform: scale(.72); opacity: .55; } }
  .stack-pills { position: absolute; bottom: 1rem; left: 1.35rem; display: flex; flex-wrap: wrap; gap: .35rem; }
  .stack-pills span { padding: .4rem .55rem; border: 1px solid rgba(255,255,255,.1); border-radius: 6px; background: rgba(9,9,10,.7); color: #d4d4d8; font: .6rem var(--font-mono); }
  .visual-bento__card--metric { display: flex; flex-direction: column; justify-content: center; background: radial-gradient(circle at 80% 10%, rgba(245,158,11,.16), transparent 50%), linear-gradient(135deg, rgba(255,255,255,.06), rgba(255,255,255,.012)); }
  .visual-bento__card--metric strong { margin: 1.2rem 0 .75rem; color: #fff; font: 800 clamp(2rem, 4vw, 3.25rem)/.96 var(--font-display); letter-spacing: -.05em; }
  .visual-bento__card--metric strong em { color: #fbbf24; font-style: normal; }
  .metric-progress { height: 5px; margin-top: 2.1rem; overflow: hidden; border-radius: 99px; background: rgba(255,255,255,.08); }
  .metric-progress span { display: block; width: 76%; height: 100%; border-radius: inherit; background: linear-gradient(90deg, #d97706, #fbbf24); animation: metric-progress 2.4s ease-in-out infinite alternate; transform-origin: left; }
  .metric-labels { display: flex; justify-content: space-between; margin-top: .55rem; color: #71717a; font: .58rem var(--font-mono); }
  @keyframes metric-progress { from { transform: scaleX(.82); } to { transform: scaleX(1); } }
  @media (max-width: 700px) { .visual-bento { padding: 4rem 1.25rem; } .visual-bento__grid { grid-template-columns: 1fr; } .visual-bento__card, .visual-bento__card--wide { min-height: 360px; } }
</style>
